# Terraform — state contains secrets in plain text
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars

# App secrets — never commit
backend/.env
.env

# Dependencies — always reinstall
node_modules/

# OS
.DS_Store
